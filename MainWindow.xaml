<Window
    x:Class="CurveAnalyzer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CurveAnalyzer"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Menu>
                <MenuItem
                    Width="150"
                    Click="GetData_Click"
                    Header="Получить данные" />
            </Menu>
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="AliceBlue"
                Orientation="Horizontal">
                <Button
                    Content="3"
                    FontFamily="Marlett"
                    FontSize="20" />
                <Label
                    Width="200"
                    Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"
                    Content="{Binding LastUpdateDate}" />
                <Button
                    Content="4"
                    FontFamily="Marlett"
                    FontSize="20" />
            </StackPanel>
        </StackPanel>
        <lvc:CartesianChart Grid.Row="1" Series="{Binding SeriesCollection}">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Labels="{Binding LabelsX}" />
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis>
                    <lvc:Axis.Separator>
                        <lvc:Separator IsEnabled="False" Step="0.25" />
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Label
                Width="250"
                Background="red"
                Content="{Binding LastUpdateDate, FallbackValue='Дата последнего обновления', StringFormat=Date: {0:ddMMyy}}" />
        </StackPanel>
    </Grid>
</Window>